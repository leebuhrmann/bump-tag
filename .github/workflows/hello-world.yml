name: Hello World

on:
    workflow_dispatch:

jobs:
    hello-world:
        runs-on: windows-latest

        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4
          with:
            token: ${{ secrets.GITHUB_TOKEN }}

        - name: Say Hello
          uses: ./hello-world.ps1